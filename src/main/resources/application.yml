server:
  port: 8080

spring:
  application:
    name: grpc-streaming-demo

grpc:
  server:
    port: 9090
  client:
    hello-service:
      address: 'static://localhost:9090'
      negotiationType: plaintext
